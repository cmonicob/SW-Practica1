<html>
	<head>
		<title>{{ username }} Sobres</title>
		<style type="text/css">
		#body {
		background-color: #DDDDDD;
		font-family: Arial;
		font-size: 12pt;
		margin: 0 auto;
		width: 100%;
		padding: 20px;	
		}
  		#p_tittle {
	    	color: black;
		font-size: 30px;
		}
	  </style>
	</head>
	<body>
		<h1>Cotxes comprats per {{ username }}</h1>
		{% if orders %}
		<ul>
		{% for order in orders %}
		<li>
		{{ order.id }} - {{ order.model.name}} - {{ order.model.marca }} - {{ order.concessionari.name }} - {{ order.model.preu }}€
		</li>
		{% endfor %}
		</ul>
		{% else %}
		<p>No ha comprat cotxes.</p>
		{% endif %}
	</body>
</html>
